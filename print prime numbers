#include<iostream>
int main()
{
	int n,i,j;
	std::cout<<"enter the number:";
	std::cin>>n;
	for(i=2;i<=n;i++)
	{
		int count=0;
		for(j=2;j*j<=i;j++)
		{
			if(i%j==0)
			{
				count++;
				break;
			}
		}
		if(count==0)
		{
			std::cout<<i<<" ";
			
		}
	}
	std::cout<<std::endl;
	return 0;
}
